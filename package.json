{
  "name": "arresta-il-sistema",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "buildWin": "npx electron-packager . arrestailsistema --platform=win32 --arch=x64 --out=dist --overwrite",
    "buildMacUniversal": "npx electron-packager . arrestailsistema --platform=darwin --arch=universal --out=dist --overwrite",
    "buildMacx86": "npx electron-packager . arrestailsistema --platform=darwin --arch=x64 --out=dist --overwrite",
    "buildMacArm": "npx electron-packager . arrestailsistema --platform=darwin --arch=arm64 --out=dist --overwrite",
    "buildLinux": "npx electron-packager . arrestailsistema --platform=linux --arch=x64 --out=dist --overwrite"
  },
  "private": true,
  "devDependencies": {
    "electron": "^37.4.0",
    "electron-packager": "^17.1.2"
  }
}
